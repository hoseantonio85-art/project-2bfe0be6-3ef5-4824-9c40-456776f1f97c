import ContentPage from "@/components/ContentPage";
import { ArrowFlow, WireframeContainer, WireframeBlock } from "@/components/WireframeBlock";

export default function Layers() {
  return (
    <ContentPage title="Слои интерфейса" subtitle="Как страница, дровер и модалка работают вместе, сохраняя контекст.">
      <p>
        Интерфейс продукта может раскрываться слоями. 
        Каждый слой добавляет глубину, не убирая предыдущий контекст. 
        Пользователь всегда знает, откуда он пришёл и куда может вернуться.
      </p>

      <ArrowFlow items={["Страница", "Дровер", "Модалка", "Вложенный дровер"]} />

      <WireframeContainer label="Визуальная схема слоёв">
        <div className="relative space-y-0">
          <div className="rounded-lg border-2 bg-card p-4">
            <span className="text-sm font-medium">Страница (основной контекст)</span>
            <div className="mt-3 ml-6 rounded-lg border-2 border-primary/30 bg-primary/5 p-4">
              <span className="text-sm font-medium text-primary">Дровер (углубление)</span>
              <div className="mt-3 ml-6 rounded-lg border-2 border-accent-foreground/20 bg-accent p-4">
                <span className="text-sm font-medium text-accent-foreground">Модалка (фокус)</span>
              </div>
            </div>
          </div>
        </div>
      </WireframeContainer>

      <h2 id="drawer">Дровер</h2>
      <p>
        Дровер (drawer) — это боковая панель, которая выезжает поверх основного контента, 
        не заменяя его. Дровер нужен, когда пользователь хочет посмотреть что-то глубже, 
        но не должен терять текущий контекст.
      </p>
      <ul>
        <li>Просмотр деталей объекта из списка</li>
        <li>Быстрое редактирование без перехода на отдельную страницу</li>
        <li>Отображение связанных данных</li>
      </ul>
      <p>
        Дровер — это способ постепенно раскрывать глубину интерфейса. 
        Пользователь видит и страницу, и дополнительную информацию одновременно.
      </p>

      <h2 id="modal">Модалка</h2>
      <p>
        Модалка создаёт фокус. Она блокирует взаимодействие с фоном 
        и требует от пользователя действия: подтвердить, заполнить, выбрать. 
        Модалка уместна, когда нужно сконцентрировать внимание на одной задаче.
      </p>
      <ul>
        <li>Подтверждение действия</li>
        <li>Короткая форма создания или редактирования</li>
        <li>Выбор из списка вариантов</li>
      </ul>

      <h2 id="nested">Вложенные сценарии</h2>
      <p>
        Интерфейс может открывать слой внутри слоя: дровер из модалки, 
        модалку из дровера, второй дровер поверх первого. 
        Это не баг — это архитектурное решение.
      </p>
      <p>
        Каждый новый слой создаёт дополнительный уровень глубины, 
        сохраняя все предыдущие слои на фоне. 
        Пользователь может вернуться на любой уровень, закрыв текущий слой.
      </p>

      <h2 id="context">Контекст и фокус внимания</h2>
      <p>
        Ключевое свойство layered interface — сохранение контекста. 
        Когда пользователь открывает дровер из списка, он по-прежнему видит список. 
        Когда открывает модалку из дровера — видит и дровер, и список за ним.
      </p>
      <p>
        Это создаёт визуальную иерархию глубины: чем ближе к пользователю слой, 
        тем больше фокуса он получает. Фоновые слои затемняются, 
        но остаются видимыми как напоминание о контексте.
      </p>
    </ContentPage>
  );
}
