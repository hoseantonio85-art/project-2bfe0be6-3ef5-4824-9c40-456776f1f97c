import ContentPage from "@/components/ContentPage";
import { ArrowFlow } from "@/components/WireframeBlock";

export default function Scenarios() {
  return (
    <ContentPage title="Сценарии" subtitle="Реальные маршруты пользователя — от первого взгляда до решения.">
      <p>
        Сценарии показывают, как принципы и паттерны работают вместе 
        в реальных задачах пользователя.
      </p>

      <h2 id="list-to-object">Из списка в объект</h2>
      <ArrowFlow items={["Список", "Строка", "Дровер / Страница объекта"]} />
      <p>
        Пользователь сканирует список, находит интересующий объект и открывает его. 
        Открытие может происходить в дровере (для быстрого просмотра) 
        или на отдельной странице (для глубокой работы). 
        Выбор зависит от задачи и контекста.
      </p>

      <h2 id="quick-view">Быстрый просмотр</h2>
      <ArrowFlow items={["Список", "Hover / Click", "Дровер", "Закрыть → Список"]} />
      <p>
        Быстрый просмотр — это сценарий, когда пользователю нужно 
        проверить несколько объектов подряд, не погружаясь глубоко в каждый. 
        Дровер открывается, показывает summary и мету, пользователь оценивает 
        и переходит к следующему объекту. Контекст списка не теряется.
      </p>

      <h2 id="action">Переход в действие</h2>
      <ArrowFlow items={["Объект", "Понимание", "Кнопка действия", "Модалка / Форма"]} />
      <p>
        Пользователь изучил объект, понял ситуацию и готов действовать. 
        Действие открывается в модалке или форме — фокус переключается 
        на конкретную задачу: подтвердить, отправить, назначить.
      </p>

      <h2 id="summary-details">От summary к деталям</h2>
      <ArrowFlow items={["Summary", "Интерес", "Раскрытие деталей", "Глубокий анализ"]} />
      <p>
        Пользователь видит summary — сжатую версию данных. 
        Если что-то требует внимания, он раскрывает подробности. 
        Если всё в порядке — переходит к следующему объекту. 
        Summary экономит время на 80% случаев, когда погружение не нужно.
      </p>

      <h2 id="decision">Принятие решения без перегруза</h2>
      <p>
        Цель всей системы — помочь пользователю принять решение. 
        Не показать все данные. Не дать все инструменты сразу. 
        А провести от сигнала через контекст к действию — с минимальными усилиями.
      </p>
      <ul>
        <li>Список показывает сигнал: «вот что требует внимания»</li>
        <li>Summary даёт контекст: «вот почему это важно»</li>
        <li>Экран объекта раскрывает детали: «вот что именно произошло»</li>
        <li>Действие завершает цикл: «вот что я с этим делаю»</li>
      </ul>
    </ContentPage>
  );
}
